To solve this I created an ArrayList and stored all the values from the nodes I then organised the ArrayList with
Collections.sort(orderNode);
finally i created the final ListNode l where i stored those values